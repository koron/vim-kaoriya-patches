support 'imaf' and 'imsf' on GTK

diff --git a/src/gui.c b/src/gui.c
index 7fc368d65..6c9dc7ff0 100644
--- a/src/gui.c
+++ b/src/gui.c
@@ -1272,7 +1272,7 @@ gui_update_cursor(
 		guicolor_T fg, bg;
 
 		if (
-# if defined(FEAT_GUI_GTK) && defined(FEAT_XIM)
+# if 0 && defined(FEAT_GUI_GTK) && defined(FEAT_XIM)
 			preedit_get_status()
 # else
 			im_get_status()
diff --git a/src/ui.c b/src/ui.c
index aa985e4ee..992cc7e56 100644
--- a/src/ui.c
+++ b/src/ui.c
@@ -1266,12 +1266,14 @@ im_save_status(long *psave)
     // And don't save when the GUI is running but our window doesn't have
     // input focus (e.g., when a find dialog is open).
     if (!p_imdisable && KeyTyped && !KeyStuffed
+#if 0
 # ifdef FEAT_XIM
 	    && xic != NULL
 # endif
 # ifdef FEAT_GUI
 	    && (!gui.in_use || gui.in_focus)
 # endif
+#endif
 	)
     {
 	// Do save when IM is on, or IM is off and saved status is on.
